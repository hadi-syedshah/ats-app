import { redirect } from "next/navigation";
import { getAuthContext } from "@/lib/ats/auth";
import { hasSupabaseConfig } from "@/lib/env";
import { Brand } from "@/components/brand";

export default async function Home() {
  if (hasSupabaseConfig()) {
    const context = await getAuthContext();
    if (context) redirect(context.profile.role === "admin" ? "/admin" : "/candidate");
  }
  return <main className="shell marketing-shell"><header className="topbar marketing-topbar"><div className="container topbar-inner"><Brand /><nav className="marketing-nav" aria-label="Primary navigation"><a href="#workflow">Workflow</a><a href="#signals">Signals</a><a className="button button-secondary button-small" href="/login">Sign in</a></nav></div></header><section className="container marketing-hero"><div className="hero-copy"><p className="eyebrow">Human signal, machine clarity</p><h1>Hiring intelligence for the people behind the <em>file.</em></h1><p className="hero-lede">Verity turns each CV into a traceable, role-specific screening record—without losing the judgment that makes hiring human.</p><div className="hero-actions"><a className="button button-primary" href="/login">Enter workspace <span aria-hidden>↗</span></a><a className="hero-text-link" href="#workflow">See the signal path <span aria-hidden>↓</span></a></div><div className="hero-signal"><span className="signal-dot" aria-hidden /> Secure, role-aware screening</div></div><div className="hero-visual" aria-label="Abstract application signal visualization"><div className="orbit orbit-one" /><div className="orbit orbit-two" /><div className="tribal-core"><span className="core-eye" /><span className="core-line line-a" /><span className="core-line line-b" /><span className="core-line line-c" /></div><div className="signal-node node-one">CV</div><div className="signal-node node-two">AI</div><div className="signal-node node-three">✓</div></div></section><section className="container signal-section" id="workflow"><div className="section-kicker"><span>01 / The signal path</span><p>Every stage leaves a readable trace.</p></div><div className="signal-grid"><article className="signal-card"><span className="signal-card-index">01</span><svg viewBox="0 0 24 24" aria-hidden><path d="M7 3h7l4 4v14H7zM14 3v5h5M10 13h4M10 17h5" /></svg><h2>Receive with rigor.</h2><p>Files are checked before they move into the hiring record.</p></article><article className="signal-card signal-card-active"><span className="signal-card-index">02</span><svg viewBox="0 0 24 24" aria-hidden><path d="M4 18V6m5 12V9m5 9V4m5 14v-7" /></svg><h2>Read the signal.</h2><p>Structured fields and role context make each CV comparable.</p></article><article className="signal-card"><span className="signal-card-index">03</span><svg viewBox="0 0 24 24" aria-hidden><path d="M5 12l4 4L19 6" /></svg><h2>Decide with context.</h2><p>Evaluations surface strengths, gaps, and a clear next action.</p></article></div></section><section className="container final-cta" id="signals"><div><p className="eyebrow">Built for deliberate decisions</p><h2>Keep the signal. Lose the noise.</h2></div><a className="button button-primary" href="/login">Sign in to Verity <span aria-hidden>↗</span></a></section><footer className="marketing-footer"><div className="container"><Brand /><p>Structured screening for thoughtful hiring.</p><span>© 2026 Verity ATS</span></div></footer></main>;
}
